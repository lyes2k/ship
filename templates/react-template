server {
    listen 80;
    listen [::]:80;  # <-- Add this line for IPv6

    server_name SUBDOMAIN.BASEDOMAIN;

    root /home/USERNAME/hosted/SUBDOMAIN;
    index index.html;

    location / {
        try_files $uri /index.html;
    }
}
